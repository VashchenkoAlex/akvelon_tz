package SequenceFinder;

/**
 * Enum class used for typification of ValidationException
 */
public enum ValidationExceptionsType {
    /**
     * Exception type if the given matrix has wrong size
     */
    WRONG_MATRIX_SIZE_EXCEPTION,
    /**
     * Exception type if the given matrix has wrong value
     */
    WRONG_VALUE_EXCEPTION
}
